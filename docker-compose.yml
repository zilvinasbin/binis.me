version: "3.8"

services:
  web:
    build: .
    container_name: binis-me
    restart: unless-stopped
    ports:
      - "4323:4323"
    environment:
      - NODE_ENV=production
    networks:
      - vps-network
      - default

networks:
  vps-network:
    external: true
